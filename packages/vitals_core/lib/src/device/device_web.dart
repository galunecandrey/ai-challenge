import 'package:flutter/foundation.dart';
import 'package:vitals_utils/vitals_utils.dart';

const PlatformInfo platformInfo = PlatformInfo(
  Platforms.web,
  // ignore: avoid_redundant_argument_values
  isProduction: kReleaseMode,
);
